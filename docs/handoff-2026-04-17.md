# Handoff — 2026-04-17

Pick up the V0 rebuild in this repo (`kinky-bubbles`). Previous session happened in `~/code/personal/event-gulper` — that repo is now obsolete and scheduled for deletion.

## What the project is

Berlin queer / kinky / conscious events aggregator. Trust-first, organizer-centric, scraping-powered. Read in order:

1. [`docs/project_brief.md`](project_brief.md) — product vision
2. [`docs/decisions/ADR-001-core-product-and-stack.md`](decisions/ADR-001-core-product-and-stack.md) — 10 load-bearing decisions (product shape + stack)
3. [`docs/plans/2026-04-17-v0-design.md`](plans/2026-04-17-v0-design.md) — V0 implementation design
4. [`docs/hipsy_analysis.md`](hipsy_analysis.md) — precedent analysis (organizer-centric patterns)

## Current state

- Branch: `rebuild/v0` (cut from `main`). **Not pushed yet.**
- Last commit: `e0f5245 scaffold: rebuild just-show-up as kinky-bubbles (V0 foundation)`
- Scaffold = Django 5.2 + Py 3.13 + Postgres 17 + pgvector, HTMX/Cotton/Tailwind v4/DaisyUI for most pages, React 19 + TS island on `/events` via django-vite, django-q2 for jobs (no Redis), pydantic-ai for LLM extraction.

## ⚠️ Three things the user needs to run before the stack boots

I couldn't execute these in the previous session (shell `cd` doesn't persist + compound commands hook-blocked). In order:

```bash
cd ~/code/personal/kinky-bubbles

# 1. Initialize beads for this repo (prefix kb)
bd init --prefix kb

# 2. Regenerate Python lockfile
uv lock

# 3. Install JS deps
cd frontend && npm install && cd ..

# 4. Boot it
cp .env.example .env   # edit secrets
docker compose -f docker-compose.yml -f docker-compose.local.yml up --build
```

The Vite dev server runs as its own compose service on `:5173`; `django-vite` auto-injects the dev bundle when `DEBUG=True`. Open http://localhost:8000.

## Open threads (unresolved decisions + pending work)

**Product/stack decisions deferred** (see ADR-001 "Open Questions" + design doc §Open Questions):
- Hosting target (Hetzner VPS / Fly.io / Railway) — affects whether pgvector + django-q2 stay cheap
- PostGIS vs plain lat/lng for venue geo
- Datastar as a future HTMX+Alpine replacement (flagged for V1 review)
- Telegram ingestion mode: bot-forward only vs also Telethon user-session

**Stale bead in event-gulper:** `eg-ddz` ("Switch to just-show-up repo and migrate V0 design docs") — this work is now done. After `bd init` in this repo, close it from the old repo with `bd --db ~/code/personal/event-gulper/.beads close eg-ddz` (or just delete the whole old repo).

**Unpushed commits on `main` in event-gulper:** `73e2108`, `fa75407`, `9a58bda` — docs that got migrated here. Probably safe to discard the old repo entirely rather than push.

## Next steps (in rough order)

1. **Verify the stack boots** — run the four commands above, confirm Django admin at `/thebaws/` and Vite HMR on `/events` both work.
2. **Beadify V0 feature checklist** from `docs/plans/2026-04-17-v0-design.md` §V0 feature checklist into `bd` epics + tasks. Use parallel subagents for efficiency (`.beads/CLAUDE.md` has the bd command reference).
3. **First migration** — the normalized schema per ADR-001 D2: Organizer, Venue, Tag (M2M), RawEvent, Event (status enum + pgvector embedding), EventImage, SourceFailure. Create Django apps `organizers`, `venues`, `events`, `ingestion`.
4. **Port the Siegessäule scraper** from `event-gulper` (3 files, stripped down): `siegessaeule.py`, `transforms/scrape.py`, `transforms/llm.py`. Drop Prefect decorators; keep pydantic-ai extraction. Wire as a django-q2 scheduled task.
5. **Delete event-gulper** once the above boots cleanly (`gh repo delete jsnyde0/event-gulper`, then `rm -rf ~/code/personal/event-gulper`).

## Name

**Kinky Bubbles** (locked this session). Soft/playful entry point to a sharper scene — matches the trust-aggregator thesis better than an abstract one-word name.

## Key non-obvious things about the stack

- **No Redis.** django-q2 uses the Postgres ORM broker. One less container.
- **Session auth + `json_script` + CSRF cookie** handles Django → React handoff. No JWT, no CORS, no Inertia.
- **DaisyUI across both surfaces** — don't add shadcn, it collides with DaisyUI CSS variables.
- **Tailwind v4 is the new pipeline** — single `frontend/src/app.css` with `@import "tailwindcss"; @plugin "daisyui"` and `@source` globs covering both Django templates and React island.
- **`pgvector/pgvector:pg17`** Postgres image — first migration needs `CREATE EXTENSION vector`.
- **Allauth 65.x uses `ACCOUNT_LOGIN_METHODS` / `ACCOUNT_SIGNUP_FIELDS`** — the old `ACCOUNT_AUTHENTICATION_METHOD` / `ACCOUNT_USERNAME_REQUIRED` settings are deprecated. Already updated.

## Persistent memory already saved

`bd remember` key: `stack-decision-validated-2026-04-17-django-monolith` — stores the full rationale for Django + HTMX + React island + Datastar-as-2026-alternative. Will surface via `bd memories` once you init the `kb` beads DB, but content is also captured in ADR-001 D5–D6 so nothing's lost.
